<?php
/**
 * REFERANS DOSYASI — config/database.php artık veritabanı
 * kimlik bilgilerini doğrudan .env / .env.local dosyasından okur.
 *
 * Bu dosyayı DÜZENLEMEYE GEREK YOK. Kurulum için:
 *
 *   1. Proje kökünde .env.example dosyasını kopyalayın:
 *        cp .env.example .env.local      (lokal geliştirme)
 *        cp .env.example .env            (üretim)
 *
 *   2. İçindeki DB_* değerlerini kendi ortamınıza göre doldurun.
 *
 * config/database.php bu değerleri env() ile okur; kodda hardcode
 * edilmiş hiçbir şifre yoktur.
 */
